name: Check data sources

# Weekly: every model, benchmark, and score source URL must still resolve.
on:
  schedule:
    - cron: "0 14 * * 1"
  workflow_dispatch:

jobs:
  sources:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm run check-sources
